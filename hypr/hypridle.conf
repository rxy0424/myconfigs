# 示例：5 分钟无操作后关闭屏幕
general {
}

listener {
    timeout = 300  # 300 秒 = 5 分钟
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}

# 可选：10 分钟后锁屏（需配合锁屏工具如 swaylock、hyprlock）
listener {
    timeout = 600
    on-timeout = "hyprlock"  # 或 "swaylock"
}
